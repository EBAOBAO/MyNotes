光敏电阻阻值与照度的关系：照度越大，阻值越小。室内正常照度时，测得电阻为 2-3 $k\Upomega$ ，照度明显偏低时，测得电阻为 9-10 $k\Upomega$ 。
设计电路原理图：



工作原理：使用运算放大器作比较器比较12两点电压，当照度大时，



由工作原理知，R4 的阻值应该在 2 $k\Upomega$ 到 9 $k\Upomega$ 之间，否则 2 点电压将恒大于或小于 1 点电压，使电路无法工作，我们取 $k\Upomega$ 的电阻作为 R4。


另外，经过多次测试，我们取阻值为  的电阻作为 R5 与 阻值为  的电阻作为 R6，并且让电路在 5V 的电压下工作，这样能保证电路工作时发光二极管与蜂鸣器的工作现象明显。